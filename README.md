# FoodLens PP v4 — Анализ калорий по фото

Учебный проект на Flask для анализа калорийности блюд по фотографиям с использованием GPT-4o Vision API. Приложение позволяет загружать фото еды, получать автоматический анализ состава, калорий и БЖУ, а также вести дневник питания с трекингом целей.

## 🎯 Основные возможности

- **Анализ фото блюд**: Загрузка фото и автоматический анализ состава, калорий и макронутриентов через GPT-4o Vision
- **Трекинг питания**: Отслеживание дневной нормы калорий и БЖУ с возможностью включения/выключения
- **Ручное добавление блюд**: Возможность добавить блюдо вручную с указанием калорий и БЖУ
- **Дашборд**: Визуализация статистики по дням, графики потребления калорий и макронутриентов
- **Профиль и цели**: Расчет дневной нормы калорий (BMR, TDEE) на основе антропометрических данных
- **Админ-панель**: Управление пользователями, просмотр статистики, назначение администраторов

## 🛠 Технологии

- **Backend**: Flask 3.0+, SQLAlchemy
- **AI**: OpenAI GPT-4o Vision API
- **Frontend**: Bootstrap 5, Chart.js
- **База данных**: SQLite
- **Обработка изображений**: Pillow

## 📋 Требования

- Python 3.8+
- OpenAI API ключ (см. раздел про API ниже)
- Интернет-соединение для работы с OpenAI API

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd food
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или установите вручную:
```bash
pip install flask flask-sqlalchemy pillow openai werkzeug python-dotenv
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your_openai_api_key_here
SECRET_KEY=your_secret_key_here
```

**Важно**: 
- Получите API ключ на [platform.openai.com](https://platform.openai.com/api-keys)
- `SECRET_KEY` — случайная строка для безопасности сессий (можно сгенерировать через `python -c "import secrets; print(secrets.token_hex(32))"`)

### 4. ⚠️ Важно: Работа с OpenAI API из России

**Проблема**: OpenAI API заблокирован в России из-за санкций. Для работы необходим VPN или прокси.

**Решения**:

1. **Использование VPN**:
   - Включите VPN перед запуском приложения
   - Убедитесь, что VPN работает стабильно
   - API запросы будут идти через VPN

2. **Использование прокси** (альтернатива):
   - Настройте прокси в коде (см. документацию OpenAI Python SDK)
   - Или используйте переменную окружения `HTTPS_PROXY`

3. **Для проверки преподавателем**:
   - Если преподаватель находится в стране без блокировки OpenAI, VPN не потребуется
   - Если преподаватель также в России, необходимо использовать VPN

**Проверка доступности API**:
```python
# Можно проверить доступность API простым запросом
from openai import OpenAI
client = OpenAI(api_key="your_key")
try:
    response = client.models.list()
    print("API доступен!")
except Exception as e:
    print(f"Ошибка доступа к API: {e}")
```

### 5. Инициализация базы данных

База данных создастся автоматически при первом запуске. Если нужно пересоздать:

```bash
# Удалите файл instance/app.db (если существует)
# При следующем запуске база создастся заново
```

### 6. Запуск приложения

```bash
python app.py
```

Приложение запустится на `http://localhost:5556` (или на адресе, указанном в `app.py`).

Для изменения хоста/порта отредактируйте последнюю строку в `app.py`:
```python
if __name__ == "__main__":
    app.run(debug=True, host='0.0.0.0', port=5556)
```

## 👤 Регистрация и вход

1. Перейдите на главную страницу
2. Нажмите "Создать аккаунт"
3. Заполните email, имя (до 60 символов) и пароль (минимум 6 символов)
4. После регистрации вы автоматически войдете в систему

## 🔐 Получение прав администратора

По умолчанию новые пользователи не являются администраторами. Чтобы получить права администратора:

### Способ 1: Через SQLite (рекомендуется)

1. Установите SQLite (если еще не установлен)
2. Откройте базу данных:
```bash
sqlite3 instance/app.db
```

3. Найдите ID вашего пользователя:
```sql
SELECT id, email, display_name, is_admin FROM user;
```

4. Установите флаг администратора:
```sql
UPDATE user SET is_admin = 1 WHERE id = YOUR_USER_ID;
```

5. Выйдите из SQLite:
```sql
.quit
```

### Способ 2: Через Python консоль

```python
from app import app, db, User

with app.app_context():
    user = User.query.filter_by(email='your_email@example.com').first()
    if user:
        user.is_admin = True
        db.session.commit()
        print(f"Пользователь {user.email} теперь администратор")
    else:
        print("Пользователь не найден")
```

### Способ 3: Через админ-панель (если уже есть админ)

Если у вас уже есть администратор, он может назначить других администраторов через админ-панель (`/admin`).

## 📊 Использование приложения

### Анализ фото блюда

1. Войдите в систему
2. Перейдите в "Загрузить"
3. Выберите фото блюда (jpg, png, webp и др.)
4. Отметьте/снимите галочку "Учитывать в трекинге целей" (по умолчанию включено)
5. Нажмите "Анализировать"
6. Дождитесь результата (может занять 10-30 секунд)

**Примечание**: Если API недоступен (нет VPN), анализ не будет работать.

### Трекинг питания

1. Заполните профиль (возраст, рост, вес, пол, активность, цель)
2. Нажмите "Включить трекинг целей" в профиле
3. Все блюда, отмеченные для трекинга, будут учитываться в дневной статистике
4. Просматривайте прогресс на дашборде и в разделе "План питания"

**Как работает трекинг**:
- Учитываются только блюда, созданные после включения трекинга
- Можно исключить отдельное блюдо из трекинга в его деталях
- Статистика обновляется в реальном времени

### Ручное добавление блюда

1. Перейдите в "Добавить вручную"
2. Заполните название (до 70 символов)
3. Укажите калории (обязательно, от 1 до 10000)
4. Укажите БЖУ (необязательно, от 0 до 1000 г каждый)
5. Укажите порцию в граммах (необязательно, от 1 до 10000)
6. Отметьте/снимите галочку для трекинга
7. Нажмите "Сохранить"

### Просмотр статистики

- **Дашборд**: Графики по дням, последние блюда, трекер за сегодня
- **План питания**: Цели (BMR, TDEE, целевые калории и БЖУ), съедено сегодня
- **Профиль**: Настройки профиля, расчет целей

## 🔧 Админ-панель

Доступна по адресу `/admin` (только для администраторов).

**Возможности**:
- Просмотр списка всех пользователей
- Статистика: общее количество пользователей, блюд, активных трекеров
- Детали пользователя: просмотр профиля, блюд, статистики
- Назначение/снятие прав администратора
- Удаление пользователей (с подтверждением)

## 📁 Структура проекта

```
food/
├── app.py                 # Основной файл приложения
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (создать вручную)
├── README.md             # Этот файл
├── instance/
│   └── app.db            # База данных SQLite (создается автоматически)
├── static/
│   ├── css/
│   │   └── app.css       # Стили приложения
│   └── uploads/          # Загруженные фото блюд
└── templates/
    ├── base.html         # Базовый шаблон
    ├── index.html        # Главная страница
    ├── login.html        # Вход
    ├── register.html     # Регистрация
    ├── dashboard.html    # Дашборд
    ├── profile.html      # Профиль
    ├── plan.html         # План питания
    ├── upload.html       # Загрузка фото
    ├── manual_add.html   # Ручное добавление
    ├── meal_detail.html  # Детали блюда
    ├── admin.html        # Админ-панель
    └── admin_user_detail.html  # Детали пользователя (админ)
```

## ⚙️ Настройки

Основные настройки находятся в начале `app.py`:

```python
OPENAI_VISION_MODEL = "gpt-4o"              # Модель для анализа фото
OPENAI_VISION_MODEL_FALLBACK = "gpt-4o-mini"  # Резервная модель
MAX_IMAGE_SIDE = 1280                        # Максимальный размер изображения
JPEG_QUALITY = 85                            # Качество JPEG
MAX_CONTENT_LENGTH = 10 * 1024 * 1024       # Максимальный размер файла (10 МБ)
```

## 🐛 Решение проблем

### API не работает

- **Проблема**: Ошибка при анализе фото
- **Решение**: Проверьте VPN подключение, убедитесь что API ключ правильный, проверьте баланс на счету OpenAI

### База данных не создается

- **Проблема**: Ошибка при первом запуске
- **Решение**: Убедитесь что папка `instance/` существует и доступна для записи

### Фото не загружаются

- **Проблема**: Ошибка при загрузке
- **Решение**: Проверьте формат файла (jpg, png, webp и др.), размер (до 10 МБ), права на запись в `static/uploads/`

### Трекинг не считает блюда

- **Проблема**: Блюда не учитываются в трекинге
- **Решение**: Убедитесь что трекинг включен в профиле, блюдо отмечено для трекинга, блюдо создано после включения трекинга

## 📝 Лицензия

Учебный проект. Не является медицинской рекомендацией.

## 👨‍💻 Автор

Учебный проект для проверки преподавателем.

---

**Примечание для проверяющего**: Если вы находитесь в стране с блокировкой OpenAI API, для тестирования функционала анализа фото потребуется VPN. Остальной функционал (ручное добавление блюд, трекинг, дашборд) работает без API.

